# Orenda (أوريندا) 🌿
**أداة تقييم نفسي تفاعلية لاكتشاف الذات واستعادة التوازن العاطفي.**

## 1. نظرة عامة على المشروع (Project Overview)
**Orenda** هو تطبيق ويب (Web App) مصمم لتقديم تقييم مبدئي لحالة المستخدم النفسية (التركيز على الاكتئاب والضغط العاطفي) استناداً إلى مقاييس علمية معتمدة مثل (PHQ-9). 
اسم المشروع مستوحى من الكلمة التي تعني "القوة الداخلية الكامنة لتغيير مسار الحياة"، مما يعكس رؤية التطبيق في تمكين المستخدمين بدلاً من إشعارهم بالمرض.

### المبادئ الأساسية (Core Principles):
1. **الخصوصية المطلقة (Zero Data Leakage):** تتم معالجة جميع الإجابات وحساب النقاط محلياً على متصفح المستخدم (Client-Side). لا يتم إرسال أو حفظ أي بيانات صحية في خوادم خارجية.
2. **العبء المعرفي المنخفض (Low Cognitive Load):** عرض سؤال واحد فقط في كل شاشة لتجنب تشتيت المستخدم.
3. **تجربة مستخدم متعاطفة (Empathetic UX):** لا يوجد مؤقتات زمنية (No Timers)، استخدام ألوان هادئة، وانتقالات سلسة بين الأسئلة.

---

## 2. البنية التقنية (Tech Stack)
* **إطار العمل الأساسي:** Next.js (App Router)
* **اللغة البرمجية:** TypeScript (لضمان قوة هيكلة البيانات وتقليل الأخطاء)
* **تنسيق الواجهات:** Tailwind CSS (لبناء تصميم متجاوب ومخصص)
* **الحركات والانتقالات:** Framer Motion (لعمل تأثيرات الانتقال السلسة Fade-in/Fade-out)
* **إدارة الحالة (State Management):** React Hooks (`useState`, `useReducer`) 

---

## 3. نظام التصميم وتجربة المستخدم (UI/UX Design System)
الهدف من التصميم هو إعطاء شعور بالاحتواء، الهدوء، والأمان.

### أ. لوحة الألوان (Color Palette)
* **لون الخلفية (Background):** أبيض كريمي دافئ `#F9F8F6` (مريح للعين، يتجنب السطوع الحاد).
* **اللون الأساسي (Primary):** أخضر مريمية هادئ `Sage Green - #8DA399` أو أزرق رمادي `Muted Indigo - #6B7A8F` (يستخدم في مؤشر التقدم والأزرار النشطة).
* **لون النصوص (Text):** رمادي فحمي `#2D3748` (بدلاً من الأسود الصريح لتقليل حدة التباين).

### ب. التفاعل (Interactions)
* **الانتقال التلقائي (Auto-Advance):** عند اختيار إجابة، ينتظر النظام `500ms` ثم ينتقل تلقائياً للسؤال التالي بحركة ناعمة، دون الحاجة لضغط زر "التالي".
* **مؤشر التقدم (Progress Bar):** شريط علوي بسيط يتحرك ببطء ليوضح للمستخدم كم تبقى من الاختبار دون إشعاره بالضغط.
* **الأزرار (Buttons):** مساحات ضغط كبيرة (Large Touch Targets) مع زوايا دائرية بالكامل (Fully rounded) `rounded-full`.

---

## 4. هيكل البيانات والمنطق البرمجي (TypeScript Schema)

هذا هو الهيكل الأساسي الذي سيُبنى عليه منطق التطبيق بالكامل:

```typescript
// 1. هيكل الإجابة المحتملة
export interface Answer {
  id: string;
  text: string;     // النص الظاهر للمستخدم (مثال: "عدة أيام")
  score: number;    // النقاط التي سيتم جمعها (مثال: 1)
}

// 2. هيكل السؤال الواحد
export interface Question {
  id: string;
  text: string;       // نص السؤال (مثال: "هل تشعر بفقدان الشغف في أداء المهام؟")
  answers: Answer[];  // مصفوفة الخيارات
}

// 3. هيكل مستويات النتيجة النهائية
export interface ResultLevel {
  minScore: number;
  maxScore: number;
  label: string;       // (مثال: "ضغط عاطفي خفيف")
  message: string;     // رسالة دعم وتشجيع مخصصة لهذه النتيجة
  actionCall: string;  // التوجيه التالي (مثال: "اقرأ هذه المقالة" أو "تحدث مع مختص")
}

// 4. حالة الاختبار (State Management)
export interface AssessmentState {
  currentQuestionIndex: number; // مؤشر السؤال الحالي
  totalScore: number;           // مجموع النقاط التراكمي
  answersRecord: Record<string, number>; // لتخزين إجابة كل سؤال تحسباً لرغبة المستخدم في الرجوع خطوة للخلف
  isCompleted: boolean;         // هل انتهى الاختبار أم لا
}